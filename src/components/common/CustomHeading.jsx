import React from 'react'

const customHeading = ({ heading}) => {
  return (
    <div>
          <h2 className={` text-[48px]   text-[#1E1E1E] leading-[110%]  text-transform: capitalize;  ${className}  `}>{ heading}</h2>
    </div>
  )
}

export default customHeading
